# 钓行使用指南

## 记录第一份档案

首页点击“记录一次钓行”，填写出钓日期、钓点和已知的作钓时长。鱼获按鱼种汇总，可补重量、钓法、饵料、照片和视频。未知数据可以留空，空军是有效记录。保存后可以从历史档案继续编辑、补充复盘和遗憾。

现场记录是可选辅助功能，不是建立档案的前提。

## 定位、天气和地图

当前位置天气需要系统定位权限及联网。不允许定位或网络失败时，仍可手工记录。地图页面需先同意高德隐私提示，平台 Key 已随安装包配置。没有坐标时可使用文字钓点。

默认天气来自 Open-Meteo。和风天气是可选高级配置，需要用户自己的 API Host、Project ID、Credential ID 和 Ed25519 私钥。历史天气按档案日期查询，失败时可手工填写。

这些联网功能会向对应服务发送所需的坐标或搜索词。官方渔轮目录更新会访问品牌官网并下载目录图片。离线档案不会因此自动上传。

## AI 识鱼

从首页右上角进入“运行配置”，填写模型服务地址、模型名和 API Key。服务需支持兼容的图片输入及 JSON 输出；纯文本模型不能用于识鱼。图片会发送到用户配置的模型服务，识别结果需由用户确认。不配置模型时，可直接从鱼种目录选择或手工填写。

模型与和风凭据保存在系统安全存储，不包含在普通档案或备份中。切勿向他人发送凭据或在问题反馈中粘贴。

## 备份、换机与升级

- 从首页“档案备份”选择文件位置，导出 ZIP 完整备份。等待成功提示后保存好文件。
- 备份包含钓行、装备及关联媒体，不包含可重新下载的官方目录或服务凭据。
- 恢复仅适用于空档案，App 会检查清单、哈希和引用。不会自动合并已有档案。
- 升级优先覆盖安装；不要先卸载。清空数据或卸载会移除本机档案。
- 本应用目前没有账号云同步，请将备份保存在可靠的外部位置。

## 分享

先预览分享内容，再选择系统分享目标。默认隐藏钓点；分享照片和支持的视频格式会清除定位元数据，处理失败则停止分享。主动输入的备注、画面中的路牌或地标仍可能透露位置，分享前请自行检查。

## 安装问题

首版安装包适用于 ARM64 Android 手机。如果显示不兼容，请反馈设备型号和系统版本。若提示签名冲突或无法覆盖安装，保留现有 App 并先导出备份，勿直接卸载或强行降级。

报告问题时提供 App 版本、系统版本、步骤和已脱敏截图即可，不要上传个人完整备份。
